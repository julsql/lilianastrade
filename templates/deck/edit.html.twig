{% extends 'base.html.twig' %}

{% block title %}Modifier Deck{% endblock %}

{% block main %}
    <h1>Modifier un deck</h1>

        {% if connect %}
            {{ include('deck/_form.html.twig', {'button_label': 'Modifier'}) }}
            {{ include('deck/_delete_form.html.twig') }}
        {% endif %}

    <a class="btn btn-primary" href="{{ path('app_deck_show', {'id': deck.id}) }}">Retour</a>

{% endblock %}
