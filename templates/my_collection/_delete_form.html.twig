<form method="post" action="{{ path('app_my_collection_delete', {'id': myCollection.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ myCollection.id) }}">
    <button class="btn btn-primary">Supprimer</button>
</form>
